This folder contains to the files to make a boot.bin file that contains the FSBL and u-boot images. 

Run this command to build boot.bin and create a new devicetree.dtb (device tree blob).

./genscript


